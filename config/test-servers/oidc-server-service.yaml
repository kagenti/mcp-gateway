apiVersion: v1
kind: Service
metadata:
  name: mcp-oidc-server
  namespace: mcp-test
  labels:
    app: mcp-oidc-server
spec:
  selector:
    app: mcp-oidc-server
  ports:
  - port: 9090
    targetPort: 9090
    protocol: TCP
