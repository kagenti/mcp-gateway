apiVersion: apps/v1
kind: Deployment
metadata:
  name: conformance-server
  labels:
    app: conformance-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: conformance-server
  template:
    metadata:
      labels:
        app: conformance-server
    spec:
      containers:
      - name: conformance-server
        image: ghcr.io/kagenti/mcp-gateway/test-conformance-server:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 9090
          name: http
        env:
        - name: PORT
          value: "9090"
        resources:
          limits:
            memory: "128Mi"
            cpu: "100m"
          requests:
            memory: "64Mi"
            cpu: "50m"
