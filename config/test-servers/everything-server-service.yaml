apiVersion: v1
kind: Service
metadata:
  name: mcp-everything-server
  labels:
    app: mcp-everything-server
spec:
  ports:
  - name: http
    port: 9090
    targetPort: 9090
  selector:
    app: mcp-everything-server
  type: ClusterIP