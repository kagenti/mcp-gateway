apiVersion: v1
kind: Secret
metadata:
  name: mcp-config-update-token
  namespace: mcp-system
type: Opaque
stringData:
  # default token for development/testing
  # in production, this should be replaced with a secure token
  token: "dev-token-replace-in-production"
