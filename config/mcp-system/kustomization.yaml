---
namespace: mcp-system
resources:
  - namespace.yaml
  - rbac.yaml
  - service.yaml
  - deployment-broker.yaml
  - deployment-controller.yaml

configMapGenerator:
  - name: mcp-gateway-config
    files:
      - config.yaml
generatorOptions:
  labels:
    app: mcp-gateway
